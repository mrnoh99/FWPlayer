name: FWPlayer
options:
  bundleIdPrefix: com.fwplayer
  createIntermediateGroups: true
  deploymentTarget:
    iOS: "17.0"

packages:
  # Pure-Swift SMB2/3 client used for direct in-app connection to SMB shares.
  # If this dependency is removed, the app still builds; the SMB feature is
  # compiled out via `#if canImport(SMBClient)` and reports as unavailable.
  SMBClient:
    url: https://github.com/kishikawakatsumi/SMBClient
    from: 0.6.0

settings:
  base:
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "1"
    SWIFT_VERSION: "5.9"
    DEVELOPMENT_TEAM: ""
    CODE_SIGN_STYLE: Automatic

targets:
  FWPlayer:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: Sources
    dependencies:
      - package: SMBClient
    entitlements:
      path: Support/FWPlayer.entitlements
    info:
      path: Support/Info.plist
      properties:
        CFBundleDisplayName: FWPlayer
        UILaunchScreen: {}
        UIBackgroundModes:
          - audio
        UIFileSharingEnabled: true
        LSSupportsOpeningDocumentsInPlace: true
        NSLocalNetworkUsageDescription: "FWPlayer connects to SMB shared drives on your local network and lets the FWPlayerRemote app discover and control playback on this device."
        NSBonjourServices:
          - _fwplayer._tcp
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.fwplayer.app
        PRODUCT_NAME: FWPlayer
        TARGETED_DEVICE_FAMILY: "1,2"
        GENERATE_INFOPLIST_FILE: NO
        ENABLE_PREVIEWS: YES
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        # Mac Catalyst: build a native Mac desktop app from the same target
        # that ships to iPhone and iPad.
        SUPPORTS_MACCATALYST: YES
        SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD: NO
        DERIVE_MACCATALYST_PRODUCT_BUNDLE_IDENTIFIER: YES
        MACOSX_DEPLOYMENT_TARGET: "14.0"

schemes:
  FWPlayer:
    build:
      targets:
        FWPlayer: all
    run:
      config: Debug
